---
alwaysApply: true
---

# SaaS Bonus System – Правила для AI-ассистента

Этот документ загружается Cursor перед каждым диалогом с ассистентом и задаёт единые правила работы для разработки SaaS платформы бонусных программ с Telegram ботами. Документацию к технологиям смотри через MCP context7.

## Архитектура проекта
- **Framework**: Next.js 15, React 19, TypeScript
- **Database**: PostgreSQL + Prisma ORM
- **UI**: Shadcn/ui + Tailwind CSS v4 + HeroUI
- **Auth**: Clerk (мультитенантность)
- **Telegram**: Grammy framework
- **API**: Next.js App Router API routes
- **OS**: Windows 10/11 + PowerShell

## Структура SaaS системы
```
SaaS Bonus System (Мультитенант)
├── Admin Dashboard (Next.js) – управление проектами
├── Webhook API – интеграция с сайтами клиентов
├── Telegram Bots (Grammy) – по одному боту на проект
├── Database (PostgreSQL) – проекты, пользователи, бонусы
└── Webhook Integration – Tilda, интернет-магазины
```

## Критические ограничения для Windows/Cursor

### 🚫 ЗАПРЕЩЕНО автоматически изменять:
- `.env` и `.env.local` файлы (заблокированы globalIgnore)
- Системные настройки Windows
- PowerShell execution policy
- Файлы в node_modules

### ✅ ОБЯЗАТЕЛЬНО делать:
1. **Env файлы**: Всегда просить пользователя создать/обновить .env файлы
2. **PowerShell команды**: Использовать PowerShell-совместимый синтаксис
3. **Пути файлов**: Использовать прямые слеши `/` или `\\` для Windows
4. **Параллельные операции**: Максимизировать параллельные tool calls
5. **Типизация**: Строгая типизация TypeScript без `any`

### 🛠️ Специфика Windows инструментов:
- `Set-Content` вместо `echo` для создания файлов
- `netstat -an | findstr :PORT` для проверки портов
- Проверка запущенных процессов через `Get-Process`
- UTF-8 encoding для всех файлов

## Ключевые принципы разработки

### 1. Мультитенантность (КРИТИЧНО)
- Каждый клиент = отдельный `Project` в БД
- Изоляция данных по `projectId`
- Уникальные webhook secrets для каждого проекта
- Отдельные Telegram боты на проект

### 2. Безопасность
- JWT токены для API аутентификации
- Webhook secret валидация
- Sanitization всех входящих данных
- Rate limiting для API endpoints

### 3. Telegram боты
- Grammy framework с типизацией
- Сессии для хранения состояния пользователя
- Обработка ошибок и fallback сценарии
- Поддержка привязки по телефону/email

### 4. Webhook API дизайн
```json
{
  "action": "register_user|purchase|spend_bonuses",
  "payload": { ... }
}
```

## Документирование процесса

### Структура документации:
- `/docs/api.md` — документация API endpoints
- `/docs/webhook-integration.md` — гайд по интеграции
- `/docs/telegram-bots.md` — настройка ботов
- `/docs/database-schema.md` — схема БД
- `/docs/changelog.md` — хронология изменений
- `/docs/tasktracker.md` — трекер задач

### Шаблон changelog
```markdown
## [YYYY-MM-DD] - Краткое описание
### 🎯 Добавлено
- 
### 🔄 Изменено
- 
### 🐛 Исправлено
- 
### 🗑️ Удалено
- 
```

### Шаблон задачи в tasktracker.md
```markdown
## 📋 Задача: [Название]
- **Статус**: ⏳ Не начата | 🔄 В процессе | ✅ Завершена | ❌ Отменена
- **Приоритет**: 🔴 Высокий | 🟡 Средний | 🟢 Низкий
- **Описание**: …
- **Техническая сложность**: 1-5
- **Затраченное время**: [X] часов
- **Шаги выполнения**:
  - [x] Выполненный шаг
  - [ ] Планируемый шаг
- **Зависимости**: Ссылки на другие задачи
- **Тестирование**: Как тестировать изменения
```

## Заголовок каждого нового файла
```typescript
/**
 * @file: [имя файла]
 * @description: [описание назначения]
 * @project: SaaS Bonus System
 * @dependencies: [ключевые зависимости]
 * @created: YYYY-MM-DD
 * @author: AI Assistant + User
 */
```

## Рабочий процесс ассистента

### До начала разработки:
1. 📋 Создать/обновить задачу в tasktracker.md
2. 🔍 Проанализировать зависимости и архитектуру
3. 💡 При неоднозначности предложить 2-3 решения

### Во время разработки:
1. 🛠️ Использовать максимально параллельные tool calls
2. 🔧 Для .env файлов - дать инструкции пользователю
3. ⚡ Проверять TypeScript типы и lint ошибки
4. 🧪 Предлагать способы тестирования

### После завершения:
1. 📝 Обновить changelog.md с датой и описанием
2. ✅ Отметить задачу как завершенную в tasktracker.md
3. 📖 Обновить соответствующую документацию
4. 🎯 Дать краткое резюме (≤5 пунктов) изменений

## Специфика SaaS Bonus System

### Базовые сущности:
- `Project` (Tenant) - клиент системы
- `User` - конечный пользователь клиента
- `Bonus` - начисленные бонусы с expiry
- `Transaction` - история операций
- `BotSettings` - настройки Telegram бота

### Типичные флоу:
1. **Регистрация**: Webhook → создание User
2. **Покупка**: Webhook → начисление Bonus → Transaction
3. **Списание**: Webhook → списание бонусов → Transaction
4. **Telegram**: привязка аккаунта → проверка баланса

### API endpoints:
- `POST /api/webhook/[secret]` - основной webhook
- `GET /api/projects` - список проектов
- `POST /api/projects` - создание проекта
- `GET /api/projects/[id]/stats` - статистика проекта

## Коммуникация с пользователем
- 🚀 Действуй проактивно, не требуй подтверждения каждого шага
- 🎯 Сохраняй контекст текущей задачи и глобальные цели
- ⚡ Указывай прогресс и оставшиеся этапы
- 🔍 Предупреждай о потенциальных проблемах заранее
- 💡 Предлагай оптимизации и улучшения

## Тестирование

### Обязательные проверки:
1. TypeScript компиляция без ошибок
2. Prisma schema валидация
3. API endpoints через curl/Postman
4. Telegram bot в test режиме
5. Webhook integration тесты

### Команды для проверки:
```powershell
# TypeScript
npx tsc --noEmit

# Prisma
npx prisma validate
npx prisma generate

# Next.js build
pnpm build

# Tests (если есть)
pnpm test
```

---
*Правила обновлены для SaaS Bonus System с учетом Windows/Cursor специфики*
- Сохраняй контекст текущей задачи и глобальную цель проекта
- Периодически напоминай о статусе и оставшихся шагах

---
*Документ объединяет прежние правила и `docs/assistant-guidelines.md` и устраняет противоречия.*