services:
  tas-panel:
    image: {{ image }}
    build: .
    ports:
      - "3000:3000"

    restart: unless-stopped
    networks:
      - nginx-public

networks:
  nginx-public:
    external: true

