/**
 * Public API of the kanban/ feature folder.
 *
 * External callers (trips-kanban-board.tsx shim, trips-listing.tsx, etc.)
 * should import only from this barrel — never from individual sub-files.
 */

export { TripsKanbanBoard } from './kanban-board';
