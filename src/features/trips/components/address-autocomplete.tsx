// Re-export so existing imports (`@/features/trips/components/address-autocomplete`) stay stable.
export {
  AddressAutocomplete,
  type AddressResult
} from './trip-address-passenger/address-autocomplete';
